object variable{
  def main(args : Array[String]) : Unit = {
    // note that 'var' can be update  mulitple times.. 
    var a : Int = 10;
    var b : String = "Hello Rash.."

    // we also have 'val' : constant
    val num : Int = 20;
    val string : String = "Rash Listen this will be only declared once during intitalization.."
    

    // Simply we can assign values using var and val only

    var x = 4923.00
    val t_ = "temp"


    // also we can assign mulitple values in one line
    val(firstName,  secondName) = ("Rashmi" , "Kuliyal")
    
  }
}